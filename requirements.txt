# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Configuration
pydantic-settings==2.6.1
python-dotenv==1.2.1

# Telephony
twilio==9.3.7

# LiveKit Agents 1.x (includes livekit-rtc and livekit-api as dependencies)
# silero = Silero VAD, turn-detector = end-of-turn detection model
livekit-agents[silero,turn-detector]~=1.4

# LiveKit AI plugins
livekit-plugins-openai~=1.4        # GPT-4o LLM + OpenAI TTS
livekit-plugins-deepgram~=1.4      # Deepgram Nova-2 STT
livekit-plugins-noise-cancellation~=0.2  # BVC/BVCTelephony noise cancellation

# Vector database
pinecone-client==5.0.1

# SQL / ORM
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0

# Redis
redis[hiredis]==5.2.0

# Task queue (async, Redis-backed)
arq==0.26.1

# HTTP client (for internal service calls)
httpx==0.27.2

# Headless browser (warranty lookup scraper — cooperandhunter.us/warranty)
playwright>=1.40.0

# Geocoding (city/state → lat/lon for technician & distributor search)
geopy==2.4.1

# Document ingestion (RAG knowledge base pipeline)
pypdf==5.1.0          # PDF parsing
python-docx==1.1.2    # DOCX parsing
python-multipart==0.0.12  # FastAPI file upload support
